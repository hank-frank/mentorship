import { HtmlTableDirective } from './html-table.directive';

describe('HtmlTableDirective', () => {
  it('should create an instance', () => {
    const directive = new HtmlTableDirective();
    expect(directive).toBeTruthy();
  });
});
